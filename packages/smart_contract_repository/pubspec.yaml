name: smart_contract_repository
description: Repository to interact with smart_contract_client
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  smart_contract_client:
    path: ../smart_contract_client

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^3.0.1
